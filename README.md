# Remote-Script-Runner
It makes possible to execute commands and scripts on a remote machine by using the WinRM service. The program works only if the WinRM service is running OR the WMI service is running.
